dist/
.astro/
node_modules/
src/assets/
package-lock.json
LICENSE
NOTICE
src/assets/fonts/licenses/

# Machine-local scratch: the Supabase Docker stack, wrangler, and what a killed test run
# leaves behind. All gitignored — named again here because Prettier does not read
# .gitignore, so without these `npm run check` fails merely because a database is running.
supabase/.temp/
supabase/.branches/
.wrangler/
.astro-build-out-*/
