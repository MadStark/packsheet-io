dist/
.astro/
node_modules/
# Supabase CLI local state — written by `supabase start`, removed by `supabase stop`.
# Gitignored as well; named here because Prettier does not read .gitignore either.
supabase/.temp/
supabase/.branches/
src/assets/
# Generated by `npm run db:types` from the local database, byte for byte as the Supabase
# CLI emits it. Formatting it would put a second definition of "correct" between the
# generator and the check in tests/database-types.test.ts: the committed file would then
# be a function of the Prettier version too, and a Prettier upgrade would read as a
# schema change. Still type-checked — tsconfig includes it, it is only the formatter and
# the linter that look away.
src/lib/database.types.ts
package-lock.json
LICENSE
NOTICE
src/assets/fonts/licenses/
