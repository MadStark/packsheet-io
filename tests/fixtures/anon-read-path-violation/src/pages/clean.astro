---
// The control case: an ordinary anonymous page that never goes near auth. Present
// so the self-test can also prove the walker does NOT flag a page that is fine —
// a checker that reports every route as a violation would pass the other two
// assertions just as well as a correct one.
---

<p>clean</p>
